#include <iostream>
using namespace std;

int main()
{
    int number;
    int output[500];
    cin >> number;
    for (int i = 1; i <= number; i++)
    {
        int numbers[4];
        for (int j = 1; j <= 4; j++)
        {
            cin >> numbers[j - 1];
        }
    }
}